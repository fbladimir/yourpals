{
  "name": "yourpals",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*", "services/*"],
  "scripts": {
    "dev:marketing": "pnpm --filter @yourpals/marketing dev",
    "dev:moneypal": "pnpm --filter @yourpals/moneypal dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": { "node": ">=20" }
}
